import styled from '@emotion/styled';
import { Select } from 'antd';

export const CustomSelectCheckout = styled(Select)({
    width: '520px',
    height: '50px',
    fontSize: '16px',
    backgroundColor: 'transparent !important',
    border: '1px solid #898989',
    borderRadius: '8px',
});
